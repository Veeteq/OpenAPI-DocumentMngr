get:
  tags:
    - Item
  summary: "Get list of items"
  description: "Get list of items"
  operationId: "listItems"
  responses:
    '200':
      description: "Success"
      content:
        application/json:
          schema:
            $ref: ../../components/schemas/v1/items.yaml
    '400':
      description: "Not found"
      content:
        application/json:
          schema:
            $ref: ../../components/schemas/Problem.yaml